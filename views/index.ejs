<!DOCTYPE html>

<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.0/normalize.css" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css" />

    </head>
    <body>

        <% let bankAccount = allBudget.reduce((newAmount, allBudget)=> { 
            return newAmount + parseInt(allBudget.amount)
         }, 0) %>

        <div class="container">
        <h1>BUDGTR</h1>
        <h3>Balance: <%= '$ ' + bankAccount %></h3>
        <table>
            <% for(let i = 0; i < allBudget.length; i++) {%>

            <tr>
                <td><%= allBudget[i].date %></td>
                <td><a href="/budget/<%=i %>"><%= allBudget[i].name %></a></td>
                <td><%= allBudget[i].amount %></td>
            </tr>

            <% } %>
        </table>
        <button><a href="/budget/new">Add New Expense</a></button>
    </div>

    <% if (bankAccount > 1000) { %>
        <style>
            h3 {
                background-color: rgb(183, 183, 231);
                display: inline-block
            }
        </style>
    <% } else if (bankAccount < 0) { %>
        <style>
            h3 {
                background-color: rgb(227, 86, 86);
                display: inline-block
            }
        </style>
   <% } %>

    </body>
</html>